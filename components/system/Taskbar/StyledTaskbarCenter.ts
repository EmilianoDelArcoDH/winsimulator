import styled from "styled-components";

const StyledTaskbarCenter = styled.div`
  bottom: 0;
  display: flex;
  height: 100%;
  left: 50%;
  max-width: calc(100vw - 260px);
  position: absolute;
  transform: translateX(-50%);
`;

export default StyledTaskbarCenter;
